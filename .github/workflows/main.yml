name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a Welcome message
        run: echo "Welcome to our first GitHub Actoions Workflow!"
      - name: Checkout repository
        uses: actions/chechout@v4.2.2
      - name: List repository files
        run: ls -la
      
